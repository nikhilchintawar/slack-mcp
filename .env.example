# Required: Slack Bot Token
SLACK_BOT_TOKEN=xoxb-your-bot-token-here

# Optional: Comma-separated list of allowed channels to search
# When set, searches are restricted to these channels only
# Use channel names (without #) or channel IDs
SLACK_CHANNELS=general,engineering,support

# Optional: Default channel ID for operations (used when no channel specified)
SLACK_DEFAULT_CHANNEL=C1234567890

# How to get your Bot Token:
# 1. Create a Slack App at https://api.slack.com/apps
# 2. Go to "OAuth & Permissions"
# 3. Add required Bot Token Scopes:
#    - channels:read (list public channels)
#    - channels:history (read public channel messages)
#    - groups:read (list private channels - optional)
#    - groups:history (read private channel messages - optional)
# 4. Install the app to your workspace
# 5. Copy the "Bot User OAuth Token" (starts with xoxb-)
# 6. Add the bot to each channel you want to search
